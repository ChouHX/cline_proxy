# 密钥与运行数据 —— 绝不要提交到仓库
# 一律加前导 / 限定在仓库根目录：不加前缀时 gitignore 会匹配任意层级，
# 那样 src/data/ 这类源码目录会被静默忽略（曾导致镜像构建时才暴露缺失）
/config.json
/metadata.json
/data/
/_testenv/

# 依赖与构建产物
node_modules/
dist/

# 系统文件
.DS_Store
Thumbs.db
